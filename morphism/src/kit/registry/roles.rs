use crate::{U0, U1};

// Role codes (type-level naturals). Registry owns these numbers.
#[allow(non_camel_case_types)]
pub type R_SYNTAX = U0;
#[allow(non_camel_case_types)]
pub type R_SEMANTICS = U1;
